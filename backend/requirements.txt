# =============================================================================
# Contract Intelligence Copilot — Backend Dependencies
# All versions pinned for reproducibility and supply-chain security.
# =============================================================================

# ── AI & LLM Orchestration ────────────────────────────────────────────────────
langchain==0.3.25
langgraph==0.2.60
langchain-groq==0.2.5           # Primary — Groq free tier (14,400 req/day, 500K tok/day)
langchain-openai==0.3.16
langchain-community==0.3.24
langchain-chroma==0.2.0
langsmith==0.3.15

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.3.1
# torch is a transitive dep — pin to avoid unexpected upgrades
torch==2.5.1

# ── Document Processing ───────────────────────────────────────────────────────
PyMuPDF==1.24.14
docling==2.15.0
pytesseract==0.3.13
python-docx==1.1.2
Pillow==11.0.0

# ── PII Detection & Anonymization ────────────────────────────────────────────
presidio-analyzer==2.2.356
presidio-anonymizer==2.2.356
spacy==3.8.3
# spaCy model installed via Dockerfile:
#   python -m spacy download en_core_web_lg

# ── Data Layer ────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
chromadb==0.5.23
redis==5.2.1
celery==5.4.0
kombu==5.4.2             # Celery messaging transport

# ── Authentication & Security ─────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
cryptography==43.0.3
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4   # Password hashing utilities

# ── API Framework ────────────────────────────────────────────────────────────
fastapi==0.115.6
pydantic==2.10.3
pydantic-settings==2.7.0
uvicorn[standard]==0.32.1
python-multipart==0.0.20  # Multipart file upload support
sse-starlette==2.1.3      # Server-Sent Events for real-time progress
httpx==0.28.1             # Async HTTP client

# ── Observability & Logging ───────────────────────────────────────────────────
structlog==24.4.0
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity==9.0.0           # Retry logic with exponential backoff
python-magic==0.4.27      # File type detection via magic bytes (not extension)
aiofiles==24.1.0          # Async file I/O
orjson==3.10.12           # Fast JSON serialization
