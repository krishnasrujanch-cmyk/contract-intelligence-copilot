"""Celery document processing task — async pipeline runner."""
from __future__ import annotations

# This file lives here so the model path is consistent.
# The actual task registration is in app/tasks/document.py (auto-discovered by Celery).
