[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "cd /app/backend && bash railway_start.sh"
restartPolicyType = "on_failure"
healthcheckPath = "/health"
healthcheckTimeout = 300
